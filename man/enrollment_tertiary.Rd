% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{enrollment_tertiary}
\alias{enrollment_tertiary}
\title{Tertiary (ensino superior) enrollment — multi-source compilation}
\format{
A tibble with approximately 1 350 rows and 16 columns
matching the canonical schema (\code{inst/dict/schema.yaml}). The
tertiary-specific columns are:
\describe{
\item{level}{Always \code{"superior"}.}
\item{network}{Administrative dependency: \code{federal}, \code{estadual},
\code{municipal}, \code{publica}, \code{privada}, plus the private sub-categories
(\code{privada_particular} / \code{privada_comunitaria_confessional_filantropica}
pre-2009, \code{privada_lucrativa} / \code{privada_nao_lucrativa} post-2009),
\code{especial}, \code{total}.}
\item{institution_type}{INEP/MEC institutional category:
\code{university}, \code{university_center}, \code{faculty},
\code{faculty_school_institute}, \code{integrated_faculty},
\code{integrated_faculty_university_center}, \code{technology_center},
\code{technology_center_fat}, \code{cefet_ifet}, \code{isolated_establishment},
\code{total}.}
\item{modality}{\code{presencial}, \code{ead}, or \code{total} (available from
2000 onwards).}
\item{is_derived}{\code{TRUE} when the row is a \strong{reconstructed total}
— a computed total combining the in-person enrollment from one
source with the EAD enrollment from INEP, used to fix the
2000-2008 transition gap where the original sources excluded
EAD from their nominal totals. Filtered out by
\code{get_enrollment()} unless \code{include_derived = TRUE}.}
}
}
\source{
Compilation curated by the package author (Mançano, 2026)
from publicly available sources. Raw file:
\code{data-raw/sources/tertiary_multisource/data_tertiary_v6_clean.xlsx}.
ETL: \verb{data-raw/03_build_enrollment_tertiary.R}.
}
\usage{
enrollment_tertiary
}
\description{
National-level (BR) Brazilian higher-education enrollment from 1907
to 2024, harmonised across seven different primary sources. The
dataset is consumed by \code{\link[=get_enrollment]{get_enrollment()}} when \code{level = "superior"}.
Multiple sources for the same \verb{(year, network)} are kept on purpose
so users can compare competing estimates — pass \code{source = "..."}
to lock in a specific series.
}
\section{Primary sources}{

\itemize{
\item \code{ibge_seculo_xx} — Anuários Estatísticos 1908-1980.
\item \code{durham_2005} — Durham (2005).
\item \code{maduro_junior_2007} — Maduro Junior MSc dissertation.
\item \code{kang_paese_felix_2021} — Kang, Paese & Felix RHE paper.
\item \code{inep_sinopse_censup} — INEP Sinopse 1995-2008.
\item \code{inep_microdados_censup} — INEP microdata 2009-2024.
\item \code{inep_censup_powerbi} — INEP Power BI panel.
}
For derived rows the \code{source} column is the concatenation
\code{"<presencial_key>+<ead_key>"}; the exact composition is documented
in \code{source_note}.
}

\keyword{datasets}
