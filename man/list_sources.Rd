% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_sources.R
\name{list_sources}
\alias{list_sources}
\title{List the data sources bundled with educabr2}
\usage{
list_sources()
}
\value{
A \code{tibble} with columns:
\describe{
\item{\code{key}}{the source key used in the \code{source} column of
\code{\link[=get_enrollment]{get_enrollment()}} / \code{\link[=get_schooling]{get_schooling()}} output.}
\item{\code{short_name}}{compact human-readable label.}
\item{\code{type}}{one of \code{"academic"}, \code{"academic_thesis"},
\code{"official_survey"}, \code{"census"}, \code{"administrative"},
\code{"administrative_microdata"}, \code{"historical_compilation"}.}
\item{\code{year_start}, \code{year_end}}{integer; \code{year_end} is \code{NA} for
ongoing series.}
\item{\code{geo}}{character; comma-separated list of geographic
levels covered (\code{"BR"}, \code{"region"}, \code{"UF"}).}
\item{\code{doi}}{character; \code{NA} when the source has none.}
\item{\code{url}}{stable URL for the source.}
\item{\code{notes}}{free-text remarks from the YAML.}
}
}
\description{
Returns a tibble describing every entry in the controlled source
vocabulary
(\code{inst/dict/vocabularies/sources.yaml}). One row per source key,
carrying short name, type, temporal/geographic coverage, DOI, URL
and free-text notes. Use this to discover which sources are
available before calling \code{\link[=educabr_cite]{educabr_cite()}} or filtering
\code{\link[=get_enrollment]{get_enrollment()}} / \code{\link[=get_schooling]{get_schooling()}} with the \code{source} argument.
}
\examples{
src <- list_sources()
head(src)

# Filter to academic papers only
src[src$type == "academic", c("key", "short_name", "doi")]

# Sources that include UF-level coverage
src[grepl("UF", src$geo), c("key", "year_start", "year_end")]

}
\seealso{
\code{\link[=educabr_cite]{educabr_cite()}}, \code{\link[=get_enrollment]{get_enrollment()}}, \code{\link[=get_schooling]{get_schooling()}}.
}
