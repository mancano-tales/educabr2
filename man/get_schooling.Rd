% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_schooling.R
\name{get_schooling}
\alias{get_schooling}
\title{Brazilian mean years of schooling}
\usage{
get_schooling(
  year = NULL,
  geo_level = c("BR", "region", "UF"),
  geo = NULL,
  dimension = c("none", "race", "sex"),
  source = NULL,
  wide = FALSE,
  lang = c("en", "pt")
)
}
\arguments{
\item{year}{Integer vector or two-element \code{c(min, max)} range. \code{NULL}
for all years.}

\item{geo_level}{One of \code{"BR"} (national, default), \code{"region"}
(macro-region), or \code{"UF"} (state). Region and UF series start in
1950.}

\item{geo}{Character vector of geographic codes. For \code{geo_level = "UF"}, 2-letter IBGE UF abbreviations (e.g. \code{"SP"}, \code{"BA"}). For
\code{geo_level = "region"}, one or more of \code{"N"}, \code{"NE"}, \code{"CO"},
\code{"SE"}, \code{"S"}. \code{NULL} (default) returns all geographies at that
level.}

\item{dimension}{Inequality breakdown. One of:
\itemize{
\item \code{"none"} (default) — national totals only (no race or sex split);
\item \code{"race"} — breakdown by IBGE color/race (\code{white}, \code{black},
\code{brown}, \code{asian}, \code{indigenous}), totals across sex;
\item \code{"sex"} — breakdown by sex (\code{male}, \code{female}), totals across race.
Race and sub-national breakdowns are only available at \code{geo_level = "BR"}.
}}

\item{source}{Character vector of source keys. \code{NULL} returns all
available sources (currently only \code{"walter_kang_2023"}).}

\item{wide}{Logical. If \code{TRUE}, pivots the result to wide form. For
this indicator the effect is minimal (only one indicator column),
but the parameter is provided for API consistency with
\code{\link[=get_enrollment]{get_enrollment()}}. Default \code{FALSE}.}

\item{lang}{One of \code{"en"} (default) or \code{"pt"}. When \code{"pt"}, factor
levels are translated via \code{inst/dict/i18n.yaml}.}
}
\value{
A tibble following the canonical schema in
\code{inst/dict/schema.yaml}. Columns: \code{year}, \code{geo_level}, \code{geo_code},
\code{geo_name}, \code{dim_race}, \code{dim_sex}, \code{age_group}, \code{indicator},
\code{value}, \code{unit}, \code{source}, \code{source_note}. The \code{level} and \code{network}
columns are omitted (not applicable to population-level attainment
averages).
}
\description{
Returns harmonized series of average years of schooling at the
national, macro-region, or state level, optionally broken down by
color/race or sex. The bundled data comes from Walter & Kang (2023),
a FGV-IBRE working paper that reconstructs the series from 1925 to
2015.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# National series, all years
get_schooling()

# By race, 1960-2015
get_schooling(dimension = "race", year = c(1960, 2015))

# By sex across states
get_schooling(dimension = "sex", geo_level = "UF", geo = c("SP", "BA"))
\dontshow{\}) # examplesIf}
}
