% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_attainment.R
\name{get_attainment}
\alias{get_attainment}
\title{Comparative international educational-attainment series}
\usage{
get_attainment(
  level = NULL,
  year = NULL,
  geo_level = c("country"),
  geo = NULL,
  dimension = c("none", "sex"),
  source = NULL,
  wide = FALSE,
  lang = c("en", "pt")
)
}
\arguments{
\item{level}{Character vector of education levels. One or more of
\code{"primary"}, \code{"secondary"}, \code{"tertiary"}. \code{NULL} (default) returns
all three.}

\item{year}{Integer vector or two-element \code{c(min, max)} range. \code{NULL}
for all years (1870–2010 in 5-year steps).}

\item{geo_level}{Always \code{"country"} for this dataset (kept for API
consistency with other \verb{get_*()} functions).}

\item{geo}{Character vector of ISO 3166-1 alpha-3 country codes (e.g.
\code{"BRA"}, \code{"USA"}, \code{"ARG"}). \code{NULL} (default) returns all 111
countries available in Lee & Lee (2016).}

\item{dimension}{Sex breakdown. One of:
\itemize{
\item \code{"none"} (default) — sex totals only (\code{dim_sex = "total"});
\item \code{"sex"} — break down by sex (\code{male}, \code{female}), drops the total.
}}

\item{source}{Character vector of source keys. \code{NULL} returns all
available sources (currently only \code{"lee_lee_2016"}).}

\item{wide}{Logical. If \code{TRUE}, pivots the result to wide form (one
column per indicator key). Default \code{FALSE}.}

\item{lang}{One of \code{"en"} (default) or \code{"pt"}. When \code{"pt"}, factor
levels and indicator labels are translated via
\code{inst/dict/i18n.yaml}. Country names (\code{geo_name}) are left in
English regardless — they come from the upstream source.}
}
\value{
A tibble in the canonical educabr2 long schema (see
\code{inst/dict/schema.yaml}). Columns: \code{year}, \code{geo_level}, \code{geo_code},
\code{geo_name}, \code{level}, \code{dim_sex}, \code{age_group}, \code{indicator}, \code{value},
\code{unit}, \code{source}, \code{source_note}. \code{geo_level} is always \code{"country"},
\code{unit} is always \code{"percent"} (0–100), \code{age_group} is always
\code{"15-64"}.
}
\description{
Returns harmonised series of educational attainment for ~111 countries,
every 5 years from 1870 to 2010, broken down by sex. The bundled data
comes from Lee & Lee (2016), \emph{Human capital in the long run} (JDE 122,
147–169) — a widely used cross-country dataset that reconstructs
attainment back to the 19th century.
}
\details{
The indicator is the \strong{cumulative} share of the population aged 15–64
that has completed at least the level indicated by \code{level}. Lee & Lee
publish the data in non-cumulative form ("highest attained level =
primary/secondary/tertiary"); the bundled dataset sums the upper
categories so that, for any (country, year, sex):
\itemize{
\item \code{level = "primary"} ≥ \code{level = "secondary"} ≥ \code{level = "tertiary"}.
}

This matches the conventional "share of adults who reached at least
X" reported in comparative work.

Coverage is comparative-international: ISCED-style \code{primary} /
\code{secondary} / \code{tertiary} levels, intentionally distinct from the
Brazilian \code{fundamental} / \code{medio} / \code{superior} levels in
\code{\link[=get_enrollment]{get_enrollment()}} and \code{\link[=get_schooling]{get_schooling()}} because the underlying
definitions differ.
}
\examples{
# Tertiary completion in Brazil over time
get_attainment(level = "tertiary", geo = "BRA")

# Primary completion across Latin America, post-1950
get_attainment(level = "primary",
               geo   = c("BRA", "ARG", "CHL", "MEX", "URY"),
               year  = c(1950, 2010))

# Compare male vs female secondary completion in Brazil
get_attainment(level = "secondary", geo = "BRA", dimension = "sex")

}
