% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_expenditure.R
\name{get_expenditure}
\alias{get_expenditure}
\title{Brazilian public expenditure on education}
\usage{
get_expenditure(
  level = NULL,
  indicator = NULL,
  year = NULL,
  source = NULL,
  wide = FALSE,
  lang = c("en", "pt")
)
}
\arguments{
\item{level}{Character vector of stage codes:
\code{"fundamental_anos_iniciais"}, \code{"fundamental_anos_finais"},
\code{"fundamental"}, \code{"medio"}, \code{"fundamental_medio"}, \code{"superior"},
\code{"total"}. The two "double ratio" indicators are tagged with
\code{level = "total"}. \code{NULL} (default) means no filter.}

\item{indicator}{Character vector. Convenience aliases are accepted:
\code{"share_gdp"}, \code{"per_student"}, \code{"double_ratio_es_ef1"},
\code{"double_ratio_es_ef_em"}. Full indicator keys
(\code{"expenditure_share_gdp"}, etc.) also work. \code{NULL} (default)
returns all four indicators.}

\item{year}{Integer vector or two-element \code{c(min, max)} range. \code{NULL}
for all years.}

\item{source}{Character vector of source keys. \code{NULL} returns all
available sources (currently only \code{"kang_menetrier_2024"}).}

\item{wide}{Logical. If \code{TRUE}, pivots the result to wide form (one
column per indicator). Default \code{FALSE}.}

\item{lang}{One of \code{"en"} (default) or \code{"pt"}. When \code{"pt"}, factor
levels and indicator labels are translated via
\code{inst/dict/i18n.yaml}.}
}
\value{
A tibble in the canonical educabr2 long schema (see
\code{inst/dict/schema.yaml}). Columns: \code{year}, \code{geo_level}, \code{geo_code},
\code{geo_name}, \code{level}, \code{network}, \code{dim_race}, \code{age_group},
\code{indicator}, \code{value}, \code{unit}, \code{source}, \code{source_note}.
\code{network} is always \code{"publica"} (public-sector expenditure only).
}
\description{
Returns the harmonised series of public expenditure on education,
compiled by Kang & Menetrier (2024) from the long-run Brazilian
national accounts and educational statistics. Three indicator
families are exposed:
}
\details{
\itemize{
\item \code{expenditure_share_gdp} — total public expenditure on a given
stage, as share of GDP.
\item \code{expenditure_per_student_pct_gdp_pc} — per-student public
expenditure in the public network, expressed as share of GDP per
capita (a unit-free, cross-country-comparable measure).
\item \code{expenditure_double_ratio_es_ef1} and
\code{expenditure_double_ratio_es_ef_em} — Kang & Menetrier's "double
ratio" indicators of fiscal regressivity (per-student spending on
tertiary divided by per-student spending on, respectively, EF1
and EF+EM combined).
}

All series are national (BR), 1933-2010. The data behind this function
is the internal dataset \link{expenditure_kang_fgv}.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Total public expenditure on education as share of GDP
get_expenditure(indicator = "share_gdp", level = "total")

# Per-student spending in tertiary education over time
get_expenditure(indicator = "per_student", level = "superior")

# The fiscal-regressivity "double ratio" — ES vs EF1
get_expenditure(indicator = "double_ratio_es_ef1")

# All indicators for 1933-1950 in wide form
get_expenditure(year = c(1933, 1950), wide = TRUE)
\dontshow{\}) # examplesIf}
}
