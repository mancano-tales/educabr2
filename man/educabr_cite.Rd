% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/educabr_cite.R
\name{educabr_cite}
\alias{educabr_cite}
\title{Generate citations for educabr2 data sources}
\usage{
educabr_cite(source_key = NULL, style = c("bibentry", "text", "bibtex"))
}
\arguments{
\item{source_key}{Character vector of source keys (e.g.
\code{"kang_paese_felix_2021"}, \code{"walter_kang_2023"}). \code{NULL} (default)
returns citations for \strong{all} sources. To discover which sources
contributed rows to a particular query, inspect the \code{source} column
of the tibble returned by \code{\link[=get_enrollment]{get_enrollment()}} or \code{\link[=get_schooling]{get_schooling()}}.}

\item{style}{One of:
\itemize{
\item \code{"bibentry"} (default) — a \code{\link[utils:bibentry]{utils::bibentry()}} object you can
further format via \code{\link[utils:toBibtex]{utils::toBibtex()}} or \code{\link[=format]{format()}};
\item \code{"text"} — APA-style prose (one character string per source);
\item \code{"bibtex"} — a \code{\link[utils:toBibtex]{utils::toBibtex()}} result ready to paste into a
\code{.bib} file.
}}
}
\value{
A \code{bibentry} object (default), a character vector, or a
\code{Bibtex} object — see \code{style}. Length is 1 per requested source.
}
\description{
Builds \code{\link[utils:bibentry]{utils::bibentry()}} objects for the primary sources whose data
is harmonised in \code{educabr2}. Use this whenever you publish analyses
built on a \verb{get_*()} call — cite the originating source(s), not only
the package itself.
}
\details{
Citation metadata is read from
\code{inst/dict/vocabularies/sources.yaml}, the same controlled vocabulary
that backs the \code{source} column of \code{\link[=get_enrollment]{get_enrollment()}} and
\code{\link[=get_schooling]{get_schooling()}}.
}
\examples{
# A single source (default returns a bibentry)
educabr_cite("kang_paese_felix_2021")

# Plain APA-style prose
educabr_cite("walter_kang_2023", style = "text")

# BibTeX entry, ready to paste into a .bib file
educabr_cite("paglayan_2022", style = "bibtex")

# All bundled sources at once
educabr_cite()

# Typical workflow: query, then cite only what you used
\dontrun{
d   <- get_enrollment(level = "fundamental", indicator = "rate")
src <- unique(d$source)
educabr_cite(src, style = "text")
}

}
\seealso{
\code{\link[=get_enrollment]{get_enrollment()}}, \code{\link[=get_schooling]{get_schooling()}},
\code{\link[utils:bibentry]{utils::bibentry()}}, \code{\link[utils:toBibtex]{utils::toBibtex()}}.
}
