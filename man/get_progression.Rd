% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_progression.R
\name{get_progression}
\alias{get_progression}
\title{Brazilian grade-progression series}
\usage{
get_progression(
  indicator = NULL,
  year = NULL,
  geo_level = c("BR", "UF"),
  geo = NULL,
  source = NULL,
  wide = FALSE,
  lang = c("en", "pt")
)
}
\arguments{
\item{indicator}{Character vector of indicator keys. Currently only
\code{"gross_distribution_ratio_grade_6"} is available; the alias
\code{"gdr6"} is also accepted. \code{NULL} (default) returns all indicators.}

\item{year}{Integer vector or two-element \code{c(min, max)} range. \code{NULL}
for all years.}

\item{geo_level}{One of \code{"BR"} (national, default) or \code{"UF"} (state).}

\item{geo}{Character vector of 2-letter UF codes when
\code{geo_level = "UF"}. \code{NULL} (default) returns all UFs available for
the indicator. \strong{Coverage note:} the source covers 20 UFs, not
all 27 — newer / territorial-origin federation units (\code{AC}, \code{AP},
\code{DF}, \code{MS}, \code{RO}, \code{RR}, \code{TO}) are not in Kang's compilation.
Passing one of those codes emits a warning explaining the gap
and returns the remaining (covered) UFs only.}

\item{source}{Character vector of source keys. \code{NULL} returns all
available sources (currently only \code{"kang_paese_felix_2021"}).}

\item{wide}{Logical. If \code{TRUE}, pivots to wide form. For this
indicator the effect is minimal (only one indicator column today),
but the parameter is provided for API consistency. Default \code{FALSE}.}

\item{lang}{One of \code{"en"} (default) or \code{"pt"}. When \code{"pt"}, factor
levels and indicator labels are translated via
\code{inst/dict/i18n.yaml}.}
}
\value{
A tibble in the canonical educabr long schema (see
\code{inst/dict/schema.yaml}). Columns: \code{year}, \code{geo_level}, \code{geo_code},
\code{geo_name}, \code{level}, \code{network}, \code{dim_race}, \code{age_group},
\code{indicator}, \code{value}, \code{unit}, \code{source}, \code{source_note}. \code{level} is
always \code{"fundamental_anos_iniciais"} and \code{unit} is always
\code{"ratio"}.
}
\description{
Returns harmonised series of grade-progression indicators in the
Brazilian school system. The bundled indicator is \strong{GDR6} (Gross
Distribution Ratio for grade 6), defined as enrollment in grades 4-6
of the old eight-year primary system divided by enrollment in grades
1-3. Higher values indicate fewer drop-outs and repeaters in the
early grades of primary education.
}
\details{
GDR6 was reconstructed by Kang, Paese & Felix (2021) for the BR
national level and for 20 federation units (UFs), 1955-2010. The
dataset behind this function is \link{progression_kang_fgv}.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# National series, all years
get_progression()

# GDR6 for São Paulo and Bahia, post-1980
get_progression(geo_level = "UF", geo = c("SP", "BA"),
                year = c(1980, 2010))

# Compare BR and the Northeast states
get_progression(geo_level = "UF",
                geo = c("BA", "PE", "CE", "PB", "MA", "PI", "RN", "AL", "SE"))
\dontshow{\}) # examplesIf}
}
